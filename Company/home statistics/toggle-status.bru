meta {
  name: toggle-status
  type: http
  seq: 2
}

get {
  url: {{company_url}}/toggle-order-status
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  is_active: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
