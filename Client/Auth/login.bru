meta {
  name: login
  type: http
  seq: 4
}

post {
  url: {{client_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "phone": "01023572563",
    "phone_code": "+20",
    "password" : "123123123"
  }
}

vars:post-response {
  client_token: res.body.body.token
}

settings {
  encodeUrl: true
  timeout: 0
}
