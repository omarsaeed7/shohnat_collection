meta {
  name: login
  type: http
  seq: 10
}

post {
  url: {{driver_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "phone": "512312312",
    "phone_code": "+966",
    "password" : "123123123"
  }
}

vars:post-response {
  driver_token: res.body.data.token
}

settings {
  encodeUrl: true
  timeout: 0
}
