meta {
  name: toggleStatus
  type: http
  seq: 1
}

post {
  url: {{driver_url}}/toggle-status
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  is_active: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
